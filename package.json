{
  "name": "invoice-reader",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "start": "npm run preview --workspace=frontend -- --port 3000 --host 0.0.0.0",
    "lint": "npm run lint --workspace=frontend --if-present"
  }
}
